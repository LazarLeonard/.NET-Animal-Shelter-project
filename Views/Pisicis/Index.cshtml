@model IEnumerable<Heaven.Models.Pisici>

@{
    ViewData["Title"] = "Index";
}



<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nume_pisica)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Varsta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Culoare)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nume_pisica)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Varsta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Culoare)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PisiciId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PisiciId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PisiciId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
