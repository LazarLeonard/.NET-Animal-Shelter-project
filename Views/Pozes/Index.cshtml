@model IEnumerable<Heaven.Models.Poze>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.id_pisica)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.id_caine)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.id_pisica)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.id_caine)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PozeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PozeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PozeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
