@model IEnumerable<Heaven.Models.Adapost>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.denumire)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.adresa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.oras)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.denumire)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.adresa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.oras)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AdapostId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AdapostId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AdapostId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
